<?xml version="1.0" encoding="utf-8"?>
<!--
  Row item for icon preset selection (WiFi / Mobile signal icons).
    • Left: preset name
    • Right: 4 small preview ImageViews showing signal states 1–4
    • On click: APPLICA / DISABILITA button (+ optional APPLICA ENTRAMBI) expands below
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Container: name + 4 preview icons (always visible) -->
    <LinearLayout
        android:id="@+id/icon_preset_row"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingVertical="14dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:background="?attr/selectableItemBackground">

        <!-- Preset name (left, expands) -->
        <TextView
            android:id="@+id/preset_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textAppearance="@style/TextAppearance.AppCompat.Medium"
            android:textColor="?attr/colorOnBackground"/>

        <!-- 4 preview icons (right-aligned, 2×2 grid) -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="end">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal">
                <ImageView
                    android:id="@+id/preview_1"
                    android:layout_width="22dp"
                    android:layout_height="22dp"
                    android:layout_marginEnd="4dp"/>
                <ImageView
                    android:id="@+id/preview_2"
                    android:layout_width="22dp"
                    android:layout_height="22dp"/>
            </LinearLayout>

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="2dp">
                <ImageView
                    android:id="@+id/preview_3"
                    android:layout_width="22dp"
                    android:layout_height="22dp"
                    android:layout_marginEnd="4dp"/>
                <ImageView
                    android:id="@+id/preview_4"
                    android:layout_width="22dp"
                    android:layout_height="22dp"/>
            </LinearLayout>
        </LinearLayout>
    </LinearLayout>

    <!-- Action buttons — hidden by default, toggle on row click -->
    <LinearLayout
        android:id="@+id/icon_preset_actions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="16dp"
        android:paddingBottom="12dp"
        android:visibility="gone">

        <!-- APPLICA when not active, DISABILITA when active -->
        <Button
            android:id="@+id/btn_apply"
            style="@style/Widget.AppCompat.Button.Colored"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#FFFFFFFF"
            android:text="@string/icon_preset_apply"/>

        <!-- APPLICA WiFi + Segnale (shown only when preset has both variants) -->
        <Button
            android:id="@+id/btn_apply_both"
            style="@style/Widget.AppCompat.Button.Borderless"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:visibility="gone"
            android:textColor="?attr/colorPrimary"
            android:text="@string/icon_preset_apply_both"/>
    </LinearLayout>

    <!-- Thin divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:layout_marginHorizontal="16dp"
        android:background="?attr/colorControlHighlight"/>

</LinearLayout>
